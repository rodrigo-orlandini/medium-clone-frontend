import Footer from '../components/Footer';

export default {
    title: "Footer",
    component: Footer
}

const Template = (args) => <Footer />;

export const Primary = Template.bind({});
Primary.args = {};