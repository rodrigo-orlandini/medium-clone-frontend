import Header from '../components/Header';

export default {
    title: "Header",
    component: Header
}

const Template = (args) => <Header />;

export const Primary = Template.bind({});
Primary.args = {};